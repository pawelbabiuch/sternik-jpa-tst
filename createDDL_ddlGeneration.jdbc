CREATE TABLE USERDETAILS (USERID VARCHAR NOT NULL, NAME VARCHAR, PASSWORD VARCHAR, VERSION BIGINT, PRIMARY KEY (USERID))
CREATE TABLE ARTICLE (ID INTEGER NOT NULL, DESCRIPTION VARCHAR, NAME VARCHAR, PRICE DOUBLE, USER_USERID VARCHAR, PRIMARY KEY (ID))
ALTER TABLE ARTICLE ADD CONSTRAINT FK_ARTICLE_USER_USERID FOREIGN KEY (USER_USERID) REFERENCES USERDETAILS (USERID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
